import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    // projectId: 'kwic0gw8',
    // dataset: 'production'
    projectId: 'zr0owxdx',
  dataset: 'production'
  }
})
